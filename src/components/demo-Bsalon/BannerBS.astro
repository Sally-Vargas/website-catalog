---
import { Image } from "astro:assets";
import bannerImage from "./BSImg/banner.jpg";

const content = {
  title: "La belleza comienza en el momento en que decides cuidarte.",
  paragraph:
    "En nuestro salón de belleza nos especializamos en resaltar lo mejor de ti. Desde cortes modernos y coloración profesional, hasta tratamientos capilares, maquillaje y cuidado de uñas, ofrecemos una experiencia completa para que te sientas segura, radiante y única. Porque la verdadera belleza está en los detalles, y nuestro objetivo es que cada visita sea un momento para consentirte.",
};
---

<section class="banner" id="banner">
  <Image
    src={bannerImage}
    alt="Banner de fondo con productos de belleza y maquillaje"
    widths={[480, 768, 1280, 1920]}
    sizes="100vw"
    quality="mid"
    class="banner-bg-image"
  />
  <div class="banner-overlay"></div>
  <div class="banner-content">
    <h1>{content.title}</h1>
    <p>{content.paragraph}</p>
  </div>
</section>

<style>
  .banner {
    position: relative; /* 1. Contenedor para elementos absolutos */
    overflow: hidden; /* 2. Evita desbordamientos */
    padding: 80px 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--text-light);
  }

  .banner-bg-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -2;
  }

  .banner-overlay {
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: -1;
  }

  .banner-content {
    position: relative; /* 3. Asegura que el contenido esté por encima del overlay */
    z-index: 1;
    max-width: 900px; /* Limitar el ancho del contenido para mejor lectura */
    width: 100%;
  }

  /* ------------------------------------------------ */
  /* TÍTULO (Escalado Fluido)                         */
  /* ------------------------------------------------ */
  .banner-content h1 {
    /* CLAVE: Usamos clamp para que el título escale suavemente: 
           Mínimo (1.8em), Preferido (4vw), Máximo (3em). */
    font-size: clamp(1.8em, 4vw, 3em);

    margin-bottom: 20px;
    /* Reducimos el espaciado de letras en móviles */
    letter-spacing: 2px;
    color: var(--accent-gold);
    line-height: 1.2;
  }

  /* ------------------------------------------------ */
  /* PÁRRAFO (Ajustes de Lectura)                     */
  /* ------------------------------------------------ */
  .banner-content p {
    /* Escala la fuente del párrafo */
    font-size: clamp(1em, 1.5vw, 1.2em);
    margin-bottom: 0; /* Ya no es necesario 30px, la altura se define con padding */
    opacity: 0.9; /* Pequeño ajuste para mejor contraste */
  }

  /* ------------------------------------------------ */
  /* MEDIA QUERY PARA MÓVILES                         */
  /* ------------------------------------------------ */

  @media (max-width: 768px) {
    .banner {
      /* Aumentamos el padding para dar más respiro visual en vertical */
      padding: 60px 20px;
    }

    .banner-content h1 {
      /* Ajuste final para móviles pequeños */
      letter-spacing: 1px;
      font-size: clamp(1.6em, 6vw, 2.5em);
    }
  }

  @media (max-width: 480px) {
    .banner {
      padding: 40px 15px; /* Más compacto en pantallas muy pequeñas */
    }
  }
</style>
